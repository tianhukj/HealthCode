name: Android CI with Ant

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        distribution: 'adopt'
        java-version: '11'

    - name: Install Ant
      run: sudo apt-get update && sudo apt-get install ant

    - name: Set up Android SDK
      uses: android-actions/setup-android@v2
      with:
        api-level: 30
        target: default
        arch: x86_64

    - name: Build with Ant
      run: ant debug

    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: HealthCodeApp-debug.apk
        path: app/bin/HealthCodeApp-debug.apk
